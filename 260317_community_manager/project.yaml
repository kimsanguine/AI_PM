id: 260317_community_manager
display_name: Community Manager
type: python-pipeline
status: active
owner: ethan
team: T2
tier: L1
summary: 커뮤니티(Discord/Slack/카카오톡) 활동 모니터링 및 자동 응답 에이전트. FAQ 자동 응답, 멤버 온보딩, 활동 리포트 생성 지원.
created: 2026-03-17
last_active: 2026-04-22

runtime: python
commands:
  setup: "python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt"
  dev: "python -m src.community_manager"
  test: "python -m pytest tests/ -v"
  build: null

paths:
  src: "src"
  tests: "tests"
  docs: "docs"
  output: "output"

policies:
  output_retention_days: 30
  cache_cleanup_level: safe
